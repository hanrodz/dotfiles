[core]
	editor = vim
	pager = less -+X -F
[user]
	name = han rodríguez
	email = han@gonzalpi.com
[alias]
	alias = config --global --get-regexp ^alias
	st = status
	cho = checkout
	cp = cherry-pick
	br = branch
	cm = commit -m
	amend = commit --amend
	noe = commit --amend --no-edit
	noedit = commit --amend --no-edit
	ln = log --oneline
	all = log --oneline --all --graph --decorate
	pruno = remote prune origin
	files = !f() { git diff HEAD HEAD~$1 --name-only; }; f
	orale = push -f
	alv = push -f
	jalese = pull
[init]
	defaultBranch = main
	autoSetupRemote = true
